import React from "react";
import Button from "./Button";
import { exitButtonProps } from "../../../constants";

function ExitButton() {
  return <Button {...exitButtonProps} />;
}

export default ExitButton;
